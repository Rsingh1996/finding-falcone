import { Destination } from "./Destination";

export const Hero = () => {
  return (
    <div className="hero-container">
      <h2>Select plants you want to search in:</h2>
      <Destination />
    </div>
  );
};
